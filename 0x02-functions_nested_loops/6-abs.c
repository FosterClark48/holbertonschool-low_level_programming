#include "main.h"
/**
 * _abs - Entry Function
 * @n: n = integer
 * Description: prints absolute value of an int
 * Return: int
 */
int _abs(int n)
{
	if (n > 0)
	{
		return (n);
	}
	else if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
