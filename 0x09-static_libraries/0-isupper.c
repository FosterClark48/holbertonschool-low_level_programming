#include "main.h"
/**
*_isupper - Used to find total no. of uppercase in sentence
*@c: upper case
*
*Return: (1) if found and (0) if not
*/
int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z')

		return (1);

	else

		return (0);

}
